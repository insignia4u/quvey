<div class="fields">
  <p>  
    <%= f.label     :title, "Question" %>
    <%= link_to_remove_fields "remove", f%><br />    
    <%= f.text_area :title, :rows => 3 %><br/>  
  </p>

  <%= f.fields_for :question_possible_values do |builder| %>  
    <%= render 'question_possible_value_fields', :f => builder %>  
  <% end %>

  <p><%= link_to_add_fields "Add Answer", f, :question_possible_values %></p>  
</div>